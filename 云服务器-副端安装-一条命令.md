# MCP 副端 · 云服务器安装（一条命令）

> 目标设备：**Linux 云服务器**（阿里云 / 腾讯云 / 华为云 ECS，或 PAI-DSW 之类容器）
> 脚本版本 **v3.0** ｜ `76250` 字节 ｜ md5 `e1d2f08ad314ca707abf0bf8be3eb28f`
> 已内嵌全部服务器代码（**零依赖**：只要系统有 `python3` 标准库，不需要 pip / npm / docker）
>
> v3.0 相对 v2.4 的三件事：**访问控制**（token + IP 白名单）、**保活守护**、
> **隧道地址自动跟随**。云服务器是暴露在公网上的，这三条都强烈建议看一遍。

---

## 一、首选：在线镜像一条命令

**把下面整段复制到云服务器终端执行**（会自动在 4 条通道里挑一条能通的）：

```bash
bash -c 'set -e; for U in \
 "https://ghfast.top/https://raw.githubusercontent.com/guoxpeng/mcp-fleet/main/install.sh" \
 "https://ghproxy.net/https://raw.githubusercontent.com/guoxpeng/mcp-fleet/main/install.sh" \
 "https://gh-proxy.com/https://raw.githubusercontent.com/guoxpeng/mcp-fleet/main/install.sh" \
 "https://raw.githubusercontent.com/guoxpeng/mcp-fleet/main/install.sh" ; do \
  echo "[*] 尝试通道: $U"; \
  if curl -fsSL --connect-timeout 15 "$U" -o /tmp/mcp-install.sh && [ "$(wc -c < /tmp/mcp-install.sh)" -gt 70000 ]; then \
    echo "[+] 下载成功 $(wc -c < /tmp/mcp-install.sh) 字节"; break; fi; done; \
 md5sum /tmp/mcp-install.sh; bash -n /tmp/mcp-install.sh && echo "[+] 语法校验通过"; \
 sudo bash /tmp/mcp-install.sh --name cloud --port 3100 --tunnel --auth-token "$(head -c 24 /dev/urandom | base64)"'
```

**通道可用性实测**（2026-09-19 从国内网络实测；`✓一致` = md5 与 `e1d2f08ad314ca707abf0bf8be3eb28f` 逐字节相同）：

| 通道 | 结果 |
|---|---|
| `ghfast.top` | HTTP 200 ｜ 76250 B ｜ ✓一致 |
| `ghproxy.net` | HTTP 200 ｜ 76250 B ｜ ✓一致 |
| `gh-proxy.com` | HTTP 200 ｜ 76250 B ｜ ✓一致 |
| `raw.githubusercontent.com` | HTTP 200 ｜ 76250 B ｜ ✓一致 |
| `cdn.jsdelivr.net` | HTTP 200 ｜ 76250 B ｜ ✓一致（**需先清缓存**，见下） |

> ⚠️ **jsDelivr 缓存坑**：它按**分支名**缓存，更新后最长 12 小时才刷新。实测本次更新后它仍在发
> v2.3 的旧版（44897 B）。要立刻拿最新版：
> ```bash
> curl "https://purge.jsdelivr.net/gh/guoxpeng/mcp-fleet@main/install.sh"   # 清缓存
> ```
> 清完再取即为新版（已实测 76250 B ✓）。所以上面的一键命令里**没把 jsDelivr 排在前面** ——
> 它需要手动 purge，不适合放在无人值守的循环里。真要绝对稳定，用 **commit SHA 的不可变直链**：
> `https://raw.githubusercontent.com/guoxpeng/mcp-fleet/<commit-sha>/install.sh`

参数说明：
- `--name cloud`：副端标识（英文，会变成服务名 `<name>-mcp`），随便改
- `--port 3100`：监听端口，没冲突就用 3100
- `--tunnel`：**云服务器必须加** —— 云主机/容器没有内网入口，脚本会自带 cloudflared 拉一条公网隧道并打印地址
- `--auth-token`：**公网设备必须加**。上面那串是当场生成的随机值；不传的话 `--tunnel` 也会自动补一个并打印出来

> 下载 cloudflared 那一步，脚本内部也自带国内镜像（`ghfast.top` → `ghproxy.net` → GitHub 官方），不用你操心。

---

## 二、保底：完全离线安装（不需要任何外网）

**适用场景**：云服务器出网被严格限制（只放行白名单），第一节几条通道全不通。

原理：把 `install.sh` 用 zlib 压缩后转 base64 → 在云服务器上**不需要联网下载任何东西**，
只要有 `python3` 就能还原。

仓库里给了**三个内容完全等价的文件**，按「你能把什么传进去」挑一个：

| 文件 | 适合怎么传 | 大小 |
|---|---|---|
| `install-offline.sh` | **推荐**。scp / U 盘 / 内网共享 —— 一个文件搞定 | 37191 B |
| `install-offline.b64` | 纯文本粘贴（网页终端、聊天窗口） | 33192 B |
| `install-offline.part01~09.txt` | 单条消息有长度限制时，分 9 段传 | 每片 4001 B |

### 2A. 自解压脚本（最省事）

```bash
# 把 install-offline.sh 传到云服务器后：
sudo bash install-offline.sh --name cloud --port 3100 --tunnel
```

脚本会自己从自身文件里切出内嵌载荷、**校验 md5**、再执行安装。不需要网络。

### 2B. 纯 base64 文本

`install-offline.b64` **里面只有 base64，没有任何注释行** —— 所以下面这条命令是整文件直接解码，
不用做任何预处理：

```bash
python3 -c "import base64,zlib;open('/tmp/mcp-install.sh','wb').write(zlib.decompress(base64.b64decode(open('install-offline.b64','rb').read())))"
md5sum /tmp/mcp-install.sh        # 应为 e1d2f08ad314ca707abf0bf8be3eb28f
bash -n /tmp/mcp-install.sh && echo "[+] 语法校验通过"
sudo bash /tmp/mcp-install.sh --name cloud --port 3100 --tunnel
```

> 正确结果应打印：`76250 字节 / md5 e1d2f08ad314ca707abf0bf8be3eb28f`

### 2C. 分片（聊天窗口分段粘贴）

9 片**全部是纯 base64**，按序号拼起来即可：

```bash
cat install-offline.part*.txt > install-offline.b64
md5sum install-offline.b64        # 与仓库里的 install-offline.b64 一致
# 然后照 2B 的命令还原安装
```

`cat` 是按文件名排序的，`part01`…`part09` 正好是正确顺序。

> 📌 **为什么这里不再内嵌一大段 base64？**
> 老版本（v2.4 及以前）把整段载荷**手工贴在本文档里**，每次改 `install.sh` 都要人工同步，
> 极易漏 —— 文档里贴的是 v2.4、仓库里发的是 v2.3 这种事故就是这么来的。
> 现在载荷由 `make-offline.py` 从 `install.sh` **自动生成**，并且能一键校验：
>
> ```bash
> python3 make-offline.py --check     # 逐字节比对 .b64 / .sh / 分片 与 install.sh
> ```
>
> 校验用的是「你实际会敲的那条命令」（整文件解码、朴素 `cat`），不是脚本自己偷偷剥注释 ——
> 所以上面三条路径是真的能跑通，不是"看起来能跑通"。

---

## 三、安全：公网设备必读（v3.0）

副端以 **root** 运行，`exec` 工具等于**把云服务器的 root shell 挂到公网上**。
v3.0 加了两道闸，**开隧道时必须至少用一道**：

| 配置 | 作用 |
|---|---|
| `auth_token` | Bearer 令牌。请求要带 `Authorization: Bearer <token>` 或 `X-Fleet-Token`。**留空 = 完全不鉴权** |
| `allow_ips` | IP/CIDR 白名单。**非空即生效**，不在名单内直接 403（在鉴权之前判） |

```bash
# 安装时指定令牌
sudo bash install.sh --name cloud --port 3100 --tunnel --auth-token '一串随机'
```

**兜底机制**：`mcp-tunnel.sh` 检测到 `auth_token` 为空，会**自动生成** 32 字节随机 token、
写进配置、重启副端，然后打印 token 和主端该加的 `headers` 片段。
真的不要这层保护（只在完全可信的内网里用）才用 `FLEET_NO_AUTH=1`（会大声警告）。

另外还有**暴力破解节流**：同一 IP 连续失败 10 次 / 60 秒窗口后，每次鉴权失败额外 `sleep 1s`。
令牌比对用 `hmac.compare_digest`（常数时间，防时序侧信道）。

> ⚠️ `allow_ips` 和公网隧道**基本互斥** —— 走隧道时来源是 cloudflared 的本机地址。
> 要公网访问就用 token。

**主端** `mcp.json` 里对应写成：

```json
{
  "mcpServers": {
    "cloud": {
      "type": "http",
      "url": "https://xxxx-xxxx.trycloudflare.com/mcp",
      "headers": { "Authorization": "Bearer 你设置的令牌" }
    }
  }
}
```

---

## 四、保活：让主端「随时可连」（v3.0）

云服务器会被 OOM、平台重启、隧道掉线打断。保活守护每 60 秒做三件事：

1. **agent 挂了 → 拉起来**（systemd 机型 `systemctl restart`，容器机型 `mcp-ctl.sh start`）
2. **隧道进程没了 → 重建**（*只在进程真的没了时* —— 能复用绝不重建，避免撞 trycloudflare 限流）
3. **把当前地址写进 `current_url.txt`，并上报「地址目录」**

systemd 机型用 oneshot service + timer，不占常驻进程：

```bash
systemctl status cloud-mcp-watchdog.timer
systemctl start  cloud-mcp-watchdog.service     # 立刻跑一轮
cat /opt/cloud_mcp/watchdog.log
```

容器机型用常驻循环（`mcp-ctl.sh start` 会自动拉起）：

```bash
bash /opt/cloud_mcp/mcp-ctl.sh watchdog         # 跑一轮 + 看守护状态
bash /opt/cloud_mcp/mcp-ctl.sh wdlog 50         # 看保活日志
bash /opt/cloud_mcp/mcp-ctl.sh keepalive off    # 关掉（或不装：--no-keepalive）
```

**地址自动跟随**：隧道域名一换，主端写死的地址就失效。两步解决：

```bash
# 1) 在一台 7x24 的机器上跑目录服务（NAS / 小主机都行）
python3 fleet-directory.py --port 8790 --token <一串随机>

# 2) 副端安装时告诉它目录在哪
sudo bash install.sh --name cloud --port 3100 --tunnel \
  --directory http://192.168.1.10:8790 --directory-token <同一串>

# 3) 主端登记目录
python3 fleet.py directory --url http://192.168.1.10:8790 --token <同一串>
```

之后主端发现地址不通会**自动问一次目录、换成新地址并回写登记表**：

```bash
python3 fleet.py sync                     # 手动刷一遍
python3 fleet.py exec cloud "uptime"      # 地址失效时自动纠正后重试
```

---

## 五、装完之后（关键一步）

脚本跑完会打印这一行：

```
公网地址（主端注册用）： https://xxxx-xxxx.trycloudflare.com/mcp
```

**把这个地址（和 token）发给我**，我在主端（你这台 Windows）执行注册 + 探测 + 真实调用验证：

```bash
# 在主端执行
python fleet.py add --name cloud --url https://xxxx-xxxx.trycloudflare.com/mcp --token <令牌>
python fleet.py probe cloud         # 握手 + 列工具 + 真调一次 exec
python fleet.py health              # 全部节点健康总览
```

`fleet.py` 是零依赖的直连 CLI，**不需要重启 WorkBuddy** 就能用。
用 WorkBuddy 原生 MCP 客户端的话，注册后需要重启 WorkBuddy 才生效。

---

## 六、日常运维

```bash
bash /opt/cloud_mcp/mcp-ctl.sh {start|stop|restart|status|log 50|tunnel|watchdog|wdlog 50|keepalive on|off}
bash /opt/cloud_mcp/mcp-ctl.sh tunnel      # 重新打印隧道地址（会复用已有隧道）
```

`mcp-ctl.sh status` 会打印服务托管方式、运行状态、保活定时器状态和当前隧道地址。

卸载：`sudo bash /tmp/mcp-install.sh --name cloud --port 3100 --uninstall`

> ⚠️ 隧道地址是临时的，容器重启就变。**配了地址目录（第四节）就不用管了**；
> 没配的话变了要重新发我，更新主端 `mcp.json`。
> ⚠️ 用完就关：`bash /opt/cloud_mcp/mcp-ctl.sh stop`。

---

## 更新日志

### v3.0（2026-09-19）— 访问控制 + 保活 + 地址自动跟随

**新增**
- **访问控制**：`auth_token`（Bearer / `X-Fleet-Token`）、`allow_ips`（CIDR 白名单）、
  鉴权失败节流（10 次/60 秒 → 每次 +1s）、`hmac.compare_digest` 常数时间比对。
  `--tunnel` 时若没 token 会自动生成并打印。
- **保活守护** `mcp-watchdog.sh`：systemd 用 oneshot + timer，容器用常驻循环。
  agent 挂了自动拉起；隧道**只在进程真没了**时才重建。`mcp-ctl.sh keepalive on|off` 可开关。
- **地址目录** `fleet-directory.py` + 主端 `fleet.py sync`：隧道换域名后主端自动跟随。
- **离线安装包重做**：由 `make-offline.py` 从 `install.sh` 自动生成
  `install-offline.sh`（自解压）/ `.b64` / `.partNN.txt`，`--check` 逐字节校验。
  不再需要手工往文档里贴 base64。

**修复**
- **离线载荷混进注释导致解码失败**：`.b64` 和分片文件里原本带 `# 说明` 头部，
  而头部里的 `MCP`、`install`、`sh`、`1/9` 这些字符**都在 base64 字母表内**，
  `b64decode` 不会丢弃它们而是当数据插进流里 → `zlib.error: incorrect header check`。
  现在载荷文件只放纯 base64，说明文字移到 `install-offline.README.txt`。
  `--check` 也改成用「用户实际会敲的命令」校验，不再自己剥注释把问题盖住。
- **重装换模式时老的 `_supervisor.sh` 和 systemd 抢端口**：日志反复刷
  `OSError: [Errno 98] Address already in use`，而 `systemctl status` 看着又是好的。
  现在 `install.sh` 启动前会 `systemctl stop` + 按安装目录 `pkill` 清干净。
- **`--auth-token` 在重装时被静默忽略**：配置文件「已存在不覆盖」的副作用。
  现在这些命令行参数会**合并进**已有配置（不传则保留原值）。
- **`mcp-ctl.sh status` 误报「未运行」**：老脚本只看 `agent.pid`，systemd 机型没这个文件。
  现在先问 `systemctl is-active`。
- **自解压脚本在 Git-Bash + 原生 Windows Python 下解出 0 字节**：
  改为让 python 读脚本自身、按 `FLEET_PAYLOAD_BEGIN/END` 标记切出载荷，并用 `cygpath` 转路径。

### v2.4（2026-09-19）— 修掉一处「重启自杀」

**症状**：通过**副端自己的 `exec` 工具**执行 `mcp-ctl.sh restart` 后，agent 再也没起来，隧道一直返回 **502**。

**根因**：`restart` 原实现是 `stop; sleep 1; start`。`stop` 会把 agent 进程（也就是这条 exec 命令的**父进程**）一起杀掉
→ 后面那句 `start` **永远执行不到**，于是「停掉了但没起来」。

**修法**：把「延迟 start」用 `setsid nohup` **脱离进程组**挂到后台，再执行 `stop`：
```bash
restart)
  if command -v setsid >/dev/null 2>&1; then
    setsid nohup bash -c "sleep 2; bash '$DIR/mcp-ctl.sh' start" >>"$DIR/ctl.log" 2>&1 </dev/null &
  else
    nohup bash -c "sleep 2; bash '$DIR/mcp-ctl.sh' start" >>"$DIR/ctl.log" 2>&1 </dev/null &
  fi
  stop ;;
```

**规则（记住）**：重启副端**要么从容器终端执行**，要么让命令自带 `setsid` 脱离进程组；
**绝不要**直接 `exec` 一条裸的 `mcp-ctl.sh restart`。

### v2.3 — 隧道默认复用已有实例；配置项 `login_shell: false`

- **PAI-DSW / Colab 等容器**：`/etc/profile.d/` 里的欢迎横幅（ASCII 图 + `Welcome to PAI DSW!`）
  会粘在**每一条命令输出**前面，污染返回值。解法：在 `mcp_agent_config.json` 里加 `"login_shell": false`
  （切成 `bash -c`，不再 source profile；PATH 仍完整）。
- 隧道脚本默认**复用已在运行的隧道**（地址不变，也避免撞 trycloudflare 频率限制）；
  要强制重建用 `CF_FORCE=1`。
